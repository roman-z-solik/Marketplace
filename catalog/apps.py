from django.apps import AppConfig


class CatalogConfig(AppConfig):
    """Класс CatalogConfig — это конфигурация приложения Django с именем catalog."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "catalog"
